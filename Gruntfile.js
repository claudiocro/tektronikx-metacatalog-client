module.exports = function (grunt) {
  'use strict';

  require('load-grunt-tasks')(grunt);

  // Project configuration.
  grunt.initConfig({

    release: {
      email: 'claudiocro@gmail.com',
      name: 'Claudio Romano'
    }

  });
};
